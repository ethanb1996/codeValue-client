import { SortbyProductsPipe } from './sortby-products.pipe';

describe('SortbyProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortbyProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
